import { BuyDirective } from './buy.directive';

describe('BuyDirective', () => {
  it('should create an instance', () => {
    const directive = new BuyDirective();
    expect(directive).toBeTruthy();
  });
});
