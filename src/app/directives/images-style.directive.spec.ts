import { ImagesStyleDirective } from './images-style.directive';

describe('ImagesStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesStyleDirective();
    expect(directive).toBeTruthy();
  });
});
